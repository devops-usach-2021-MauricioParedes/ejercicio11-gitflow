//import '@testing-library/jest-dom'
//import usuarios from './data/usuarios';

/*describe("Pruebas unitarias",()=>{
    test('Formato pruebas',()=>{
        
        const usuarioFalso ={
          id:99,
          nombre: 'falso',
          correo: 'falso'
        };
        const usuario=usuarios.find(p=>p.id==usuarioFalso.id);
        //expect(usuario).toEqual(usuarioFalso);
       
    })

   

})*/
